1. Les fichiers MNIST et ADULT ne sont pas dans cette répertoire en
raison de la grosseur de la taille de ses fichiers. 
2. MNIST: Compiler le fichier mnist.py
3. ADULT: Compiler le fichier adult.py
4. Le notebook résume tout le code des fichiers MNIST et ADULT en un
   seul notebook pour facilier la visualisation. En outre, on remarque
   PyCharm nous retourne une erreur lorsqu'on veut calculer les prédictions
   d'Isomap avec distance euclidienne. Cependant, cette erreur n'est pas
   survenue dans le notebook, donc c'est pour cela qu'on garde le notebook
   afin que tous les exemples et résultats soient bien présents.
5. Le fichier algorithms.py contient toutes les mesures de similarité
   que nous avons implémentée ainsi que le calcul de la matrice de la
   dissimilarité et les cinq algorithmes (kNN et les quatre autres
   méthodes non supervisées).